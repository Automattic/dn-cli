{
  "plugins": [
    {
      "name": "domain-names",
      "description": "Manage domains from Claude Code — check availability, register, configure DNS, and more via the dn CLI",
      "source": "."
    }
  ]
}
